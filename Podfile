# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SafePrintScreenSwift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SafePrintScreenSwift

  target 'SafePrintScreenSwiftTests' do
    # Pods for testing
  end

end
